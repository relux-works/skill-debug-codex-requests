interface:
  display_name: "Debug Codex Requests"
  short_description: "Capture and inspect Codex requests"
  default_prompt: "Use $debug-codex-requests to run the proxied Codex workflow end-to-end, inspect the captured log, and summarize what Codex actually sent."
